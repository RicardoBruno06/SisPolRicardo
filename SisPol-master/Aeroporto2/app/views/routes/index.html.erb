<p id="notice"><%= notice %></p>

<h1>Listando Rotas</h1>

<table>
  <thead>
    <tr>
      <th>Hora</th>
      <th>Preço</th>
      <th>Status</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @routes.each do |route| %>
      <tr>
        <td><%= route.hour %></td>
        <td><%= route.price %></td>
        <td><%= route.status %></td>
        <td><%= link_to 'Mostrar', route %></td>
        <td><%= link_to 'Editar', edit_route_path(route) %></td>
        <td><%= button_to 'Destruir', route, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nova rota', new_route_path %>
